<Window x:Class="Simply.Reactive.Wpf.Tests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:r="clr-namespace:Simply.Reactive.Wpf.Xaml;assembly=Simply.Reactive.Wpf"
        DataContext="{StaticResource MainWindowViewModel}"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Row="0" Grid.Column="0" Content="IsWorking" IsChecked="{r:ReactiveBinding Path=IsWorking}" />
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{r:ReactiveBinding Path=IsWorkingDescription}" Width="200" />
        <CheckBox Grid.Row="1" Grid.Column="0" Content="IsWorking2" IsChecked="{r:ReactiveBinding Path=IsWorking2}" />
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{r:ReactiveBinding Path=IsWorking2Description}" Width="200" />
    </Grid>
</Window>
